git hub config

git config --global user.name "aminmansuri123"

git config --global user.email "mansuri.amin1@gmail.com"

git config --global color.ui auto

mandatory commands

for intializing

   git init

for repo connection
   git remote add origin https://github.com/aminmansuri123/Learning_Python_tutedude.git


pull request

   git pull origin main

status of application
   git status

add files
   git add .  # for all files
   git add exc1 ex2   for particular files`

   after add run
   git status

   then
   git commit m "Commit msg"

   then
   git push

   git push --set-upstream origin master
test
